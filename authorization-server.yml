spring:
  datasource:
    url:      jdbc:postgres://localhost:5432/auth_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

---
spring:
  datasource:
    url:      jdbc:postgres://postgres:5432/auth_db
    password: ${POSTGRES_PASSWORD}